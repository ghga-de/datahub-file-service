[project]
name = "dhfs"
version = "4.0.0"
description = "Data Hub File Service - a service enabling file inspection and re-encryption at Data Hubs"
dependencies = [
    "typer >= 0.25.1",
    "ghga-service-commons[auth,http,objectstorage,transports,crypt] >= 8.0.0",
    "hexkit[s3] >= 9.0.0",
    "crypt4gh >= 1.7",
    "async-lru >= 2.1.0",
]

[project.urls]
Repository = "https://github.com/ghga-de/datahub-file-service"

[project.scripts]
dhfs = "dhfs.__main__:run"
